const inbox = [4, -5, 4, 0];
const outbox = [];

for (var i = 0; i < inbox.length; i++) {

    const box = inbox[i] * 8;
    const total= box

    outbox.push(total);
}

console.log('Outbox:', outbox);
