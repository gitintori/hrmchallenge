const inbox = [2, -5, 6, 0];
const outbox = [];

for (var i = 0; i < inbox.length; i++) {

    const box = inbox[i] * 40;
    const total= box

    outbox.push(total);
}

console.log('Outbox:', outbox);
