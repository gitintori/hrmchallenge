const inbox = [6, -1, 1, 0];
const outbox = [];

for (var i = 0; i < inbox.length; i++) {

    const box = inbox[i] * 3;
    const total= box

    outbox.push(total);
}

console.log('Outbox:', outbox);